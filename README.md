# Space-image-bot
Проект позволяет скачивать и публиковать фотографии космоса из разных источников: SpaceX, NASA APOD и NASA EPIC.
Фотографии можно автоматически публиковать в Telegram-канал с возможностью периодической публикации.

Проект был создан в учебных целях для сайта [dvmn.org](https://dvmn.org).

## **📦 Возможности проекта**

1. **Скачивание фото запуска SpaceX**

- По умолчанию скачивает фото последнего запуска.

- Можно указать ID конкретного запуска.

2. **Скачивание APOD-фото NASA**

- Поддерживается массовое скачивание 30–50 фото за один запрос.

- Автоматически использует DEMO_KEY, если основной API-ключ недоступен.

3. **Скачивание EPIC-фото NASA**

- Фото нашей планеты в формате PNG.

- Поддерживается массовое скачивание нескольких снимков.

4. **Публикация фото в Telegram**

- Можно указать конкретную фотографию или публиковать случайное фото.

- Возможна автоматическая публикация всех фото из директории с повторением и случайным перемешиванием.

5. **Вспомогательные функции**

- ```download_image(url, path)``` – скачивает картинку по URL.

- ```get_file_extension(url)``` – определяет расширение картинки.

## **⚙️ Установка**

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/doingmov/Space-image-bot
cd Space-image-bot
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте .env и добавьте свои ключи:**
```bash
TELEGRAM_TOKEN=ваш_токен_бота
NASA_API_KEY=ваш_nasa_api_key
```

## **🖼️ Скачивание фото**

**SpaceX**

- Скачать фото последнего запуска:
```bash
python fetch_spacex_images.py
```

- Скачать фото по конкретному ID запуска:
```bash
python fetch_spacex_images.py --launch_id 5eb87d47ffd86e000604b38a
```

Фотографии сохраняются в папку ```images/```.

**NASA APOD**

- Скачать фотографии дня:
```bash
python fetch_nasa_apod.py --api_key $NASA_API_KEY --count 30
```

Фотографии сохраняются в папку ```nasa_images/```.

**NASA EPIC**

- Скачать фото Земли:
```bash
python fetch_nasa_epic.py --api_key $NASA_API_KEY --count 10
```

Фотографии сохраняются в папку ```epic_images/```.

## **🤖 Публикация в Telegram**

**Публикация одной фотографии**
```bash
python publish_photo.py --token $TELEGRAM_TOKEN --chat_id @ваш_канал --photo path/to/photo.jpg
```
- Если ```--photo``` не указан, публикуется случайная фотография из папки по умолчанию.

**Автоматическая публикация всех фото из директории**
```bash
python auto_publish.py --token $TELEGRAM_TOKEN --chat_id @ваш_канал --dir images --delay 4
```

- ```--dir``` – папка с изображениями (по умолчанию ```images/```).

- ```--delay``` – задержка между публикациями в часах (по умолчанию 4).

- Скрипт запускается бесконечно и перемешивает фотографии случайным образом.

## **🔧 Настройка**

- Ключи API хранятся в .env.

- Скрипты можно запускать отдельно или через main.py, который скачивает все типы фото разом:
```bash
python main.py
```
- Папки с фото создаются автоматически при скачивании:
```
images/       -> SpaceX
nasa_images/  -> APOD NASA
epic_images/  -> EPIC NASA
```

## *💡 Советы*

1. Telegram ограничивает размер файлов до 20 MB.

2. DEMO_KEY — это публичный токен с ограничением по количеству запросов (до 30 фото в час).  
   Используйте свой токен NASA_API_KEY для полного доступа.  
   Если токен неверный или превышен лимит — скрипт сообщит об этом.

3. ```.env``` и скачанные фото не включаются в репозиторий.
